# RIPPLE CPU — 产品需求文档 (PRD)

## 文档信息

| 属性 | 内容 |
|------|------|
| **产品名称** | RIPPLE |
| **文档版本** | v1.0 |
| **文档状态** | 初稿 |
| **创建日期** | 2026-08-24 |
| **目标完成** | 50+ 节完整规范 |

---

## 目录

1. [产品概述](#1-产品概述)
2. [设计目标与指标](#2-设计目标与指标)
3. [指令集架构支持](#3-指令集架构支持)
4. [微架构总体设计](#4-微架构总体设计)
5. [前端模块设计](#5-前端模块设计)
6. [执行单元设计](#6-执行单元设计)
7. [乱序核心设计](#7-乱序核心设计)
8. [缓存子系统设计](#8-缓存子系统设计)
9. [内存管理单元](#9-内存管理单元)
10. [总线接口](#10-总线接口)
11. [时钟与功耗管理](#11-时钟与功耗管理)
12. [调试与追踪](#12-调试与追踪)
13. [编码规范与风格](#13-编码规范与风格)
14. [验证策略](#14-验证策略)
15. [待确认事项](#15-待确认事项)

---

## 1. 产品概述

### 1.1 产品定位

**RIPPLE** 是一款高性能超标量乱序执行（OoO）RISC-V CPU 核心 IP，面向桌面级应用场景。设计目标是在 1-2GHz 工作频率下实现 IPC 1.5-2.0 的性能，支持完整的 Linux 操作系统运行。

### 1.2 核心特性

| 特性 | 描述 |
|------|------|
| **架构** | RV64 超标量乱序执行核心 |
| **发射宽度** | 4-宽（每周期最多发射4条指令） |
| **多线程** | 支持 2-way SMT（同时多线程） |
| **指令集** | G扩展 + 向量(V) + 压缩(C) + 预取 |
| **特权级** | M-mode + S-mode + U-mode |
| **虚拟化** | 支持 Sv39 三级页表 |

### 1.3 应用场景

- 桌面级 SoC 核心
- 高性能嵌入式处理器
- 需要运行完整 RISC-V Linux 的系统
- 追求性能/功耗平衡的移动设备

---

## 2. 设计目标与指标

### 2.1 性能指标

| 指标 | 目标值 | 说明 |
|------|--------|------|
| **工作频率** | 1.0 - 2.0 GHz | 目标频率范围 |
| **IPC** | 1.5 - 2.0 | 每周期指令数 |
| **CPI** | 0.5 - 0.67 | 每条指令周期数 |
| **分支预测准确率** | > 98% | TAGE主预测器 |
| **Load-to-Use 延迟** | 4 cycles | L1命中情况 |

### 2.2 面积与功耗

| 指标 | 目标值 | 说明 |
|------|--------|------|
| **核心面积** | ~2-3 mm² (14nm) | 预估 |
| **功耗预算** | 2-5W | 平衡设计 |
| **SMT面积增加** | +25-30% | 相对单线程 |

### 2.3 性能对比参考

| 参考处理器 | IPC | 频率 | 说明 |
|-----------|-----|------|------|
| SiFive P870 | ~1.5 | 1.5GHz | 6-宽乱序 |
| BOOM v3 | ~1.8 | 2.0GHz | 开源高性能核心 |
| RIPPLE (目标) | 1.5-2.0 | 1.0-2.0GHz | 本设计 |

---

## 3. 指令集架构支持

### 3.1 基础整数集

| 扩展 | 状态 | 说明 |
|------|------|------|
| RV64I | ✅ 必选 | 64位基础整数指令集 |
| Zifencei | ✅ 必选 | 指令栅栏 |
| Zicsr | ✅ 必选 | 控制状态寄存器 |
| Ztrap | ✅ 必选 | 陷阱处理增强 |

### 3.2 乘法与除法

| 扩展 | 状态 | 说明 |
|------|------|------|
| M 扩展 | ✅ 必选 | 乘除法指令 |
| Zmmul | ✅ 必选 | 仅乘法（优化） |

### 3.3 浮点扩展

| 扩展 | 状态 | 说明 |
|------|------|------|
| F 扩展 | ✅ 必选 | 单精度浮点 |
| D 扩展 | ✅ 必选 | 双精度浮点 |
| Zfinx | ✅ 必选 | 整数寄存器存浮点 |
| Zfa | 🔲 可选 | 浮点附加指令 |
| Zfhmin | 🔲 可选 | 半精度最小支持 |

### 3.4 压缩与扩展

| 扩展 | 状态 | 说明 |
|------|------|------|
| C 扩展 | ✅ 必选 | 压缩指令，减小代码体积 |
| Zba | ✅ 必选 | 地址计算加速 |
| Zbb | ✅ 必选 | 位操作 |
| Zbs | ✅ 必选 | 移位掩码 |
| Zkt | ✅ 必选 | 密码学工具 |

### 3.5 向量扩展

| 配置 | 规格 | 说明 |
|------|------|------|
| **扩展版本** | V 扩展 (Zve64x) | 最小硬件：64位 |
| **ELEN** | 64 bits | 最大元素宽度 |
| **VLEN** | 64 bits | 寄存器宽度 |
| **LMUL** | 1, 2, 4, 8 | 多种向量长度 |
| **SEW** | 8-64 bits | 动态元素宽度 |

### 3.6 预取扩展

| 扩展 | 状态 | 说明 |
|------|------|------|
| Zicbop | ✅ 必选 | 缓存预取提示 |
| Zicboz | ✅ 必选 | 缓存零化操作 |
| Zicbom | ✅ 必选 | 缓存维护操作 |

### 3.7 特权级支持

| 模式 | 状态 | 说明 |
|------|------|------|
| M-mode | ✅ 必选 | 机器模式（最高特权） |
| S-mode | ✅ 必选 | 管理模式（虚拟化支持） |
| U-mode | ✅ 必选 | 用户模式 |
| H-extension | ✅ 必选 | 虚拟化扩展 |

### 3.8 其他扩展

| 扩展 | 状态 | 说明 |
|------|------|------|
| Sv39 | ✅ 必选 | 39位虚拟地址分页 |
| Zicount | ✅ 必选 | 性能计数器 |
| Ztune | 🔲 可选 | 性能调优寄存器 |
| S 扩展 (Debug) | ✅ 必选 | 调试支持 |

---

## 4. 微架构总体设计

### 4.1 流水线阶段

**RIPPLE 核心采用经典的乱序执行流水线：**

```
┌─────────────────────────────────────────────────────────────────────────┐
│                         前端 (Frontend)                                  │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐  │
│  │ 分支预测 │→│ 指令取指 │→│ 指令解码 │→│ 重命名   │→│ 调度发射 │  │
│  │ (TAGE)   │  │ (Fetch)  │  │ (Decode) │  │ (RAT)    │  │ (Issue)  │  │
│  └──────────┘  └──────────┘  └──────────┘  └──────────┘  └──────────┘  │
└─────────────────────────────────────────────────────────────────────────┘
                                    ↓
┌─────────────────────────────────────────────────────────────────────────┐
│                         执行 (Execute)                                   │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐  │
│  │ 整数ALU  │  │ 乘法单元 │  │ 除法单元 │  │ 分支单元 │  │ 向量单元 │  │
│  │ (×4)     │  │ (×2)     │  │ (×1)     │  │ (×2)     │  │ (共享)   │  │
│  └──────────┘  └──────────┘  └──────────┘  └──────────┘  └──────────┘  │
│  ┌──────────┐  ┌──────────┐                                              │
│  │ 加载单元 │  │ 存储单元 │                                              │
│  │ (×2)     │  │ (×1)     │                                              │
│  └──────────┘  └──────────┘                                              │
└─────────────────────────────────────────────────────────────────────────┘
                                    ↓
┌─────────────────────────────────────────────────────────────────────────┐
│                         后端 (Backend)                                   │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐                              │
│  │ 指令完成 │→│ ROB提交  │→│ 写回寄存器│                              │
│  │ (Writeback)│ │ (Commit) │ │ (Retire) │                              │
│  └──────────┘  └──────────┘  └──────────┘                              │
└─────────────────────────────────────────────────────────────────────────┘
```

### 4.2 关键设计参数

| 参数 | 配置 | 说明 |
|------|------|------|
| **发射宽度** | 4 instructions/cycle | 超标量宽度 |
| **最大指令窗口** | 96 entries | ROB容量 |
| **SMT线程数** | 2 threads | 同时多线程 |
| **流水线级数** | ~15-20 stages | 包括执行单元流水 |

### 4.3 设计理念

1. **平衡设计**：面积、功耗、性能三者平衡
2. **可扩展性**：模块设计支持未来扩展
3. **可验证性**：模块化设计便于验证
4. **低延迟**：关键路径优化，减少流水线停顿

---

## 5. 前端模块设计

### 5.1 分支预测器 (Branch Predictor)

#### 5.1.1 总体架构

| 组件 | 配置 | 说明 |
|------|------|------|
| **主预测器** | TAGE-SC-L | 高精度预测器 |
| **辅助预测器** | Perceptron | 针对TAGE难以预测分支 |
| **间接跳转预测** | ITAGE | 间接跳转目标预测 |
| **返回栈** | RAS (32 entries) | 函数调用返回预测 |
| **分支目标缓冲** | BTB (4K entries) | 缓存分支目标地址 |
| **依赖识别** | 长城依赖检测 | 强相关分支依赖识别 |

#### 5.1.2 TAGE 预测器配置

| 参数 | 值 | 说明 |
|------|-----|------|
| **预测器名称** | TAGE-SC-L | TAgged GEometric length predictor with Statistical Correction and Loop detector |
| **历史长度** | 5-11 (几何级数) | 不同长度的历史窗口 |
| **表数量** | 8 个 tag tables | 对应不同历史长度 |
| **Tag宽度** | 8-12 bits | 标签宽度 |
| **预测延迟** | 2 cycles | 预测计算延迟 |

#### 5.1.3 BTB 配置

| 参数 | 值 | 说明 |
|------|-----|------|
| **容量** | 4096 entries | 4K条目 |
| **相联度** | 4-way | 四路组相联 |
| **Tag宽度** | ~40 bits | 标签位宽 |
| **数据宽度** | ~40 bits | 目标地址 |

#### 5.1.4 RAS 配置

| 参数 | 值 | 说明 |
|------|-----|------|
| **深度** | 32 entries | 返回栈深度 |
| **操作** | push/pop | 函数调用/返回 |
| **预测延迟** | 1 cycle | 快速返回预测 |

#### 5.1.5 性能指标

| 指标 | 目标值 | 说明 |
|------|--------|------|
| **分支预测准确率** | > 98.5% | 平均MPKI < 5 |
| **间接跳转准确率** | > 90% | ITAGE增强后 |
| **返回栈命中率** | > 99% | RAS预测 |

### 5.2 指令取指 (Instruction Fetch)

#### 5.2.1 取指单元

| 参数 | 配置 | 说明 |
|------|------|------|
| **取指宽度** | 4 instructions/cycle | 每周期4条指令 |
| **缓冲区** | 16 entries Fetch Buffer | 取指缓冲 |
| **地址计算** | 专用 ALU | PC+4 计算 |
| **L1I访问** | 4-cycle latency | 命中延迟 |

#### 5.2.2 指令对齐与拆分

- 支持 4/2/1 字节指令混合
- C扩展指令 (RVC) 自动检测
- 指令边界自动对齐

### 5.3 指令解码 (Instruction Decode)

| 参数 | 配置 | 说明 |
|------|------|------|
| **解码宽度** | 4 instructions/cycle | 每周期解码4条 |
| **解码器** | 4 个并行解码器 | 每个ALU一个 |
| **功能分类** | ALU/MUL/DIV/LSU/BRU/PU | 指令分类 |
| **依赖检测** | 数据/控制依赖检测 | 前递与停顿控制 |

---

## 6. 执行单元设计

### 6.1 整数 ALU 单元

#### 6.1.1 ALU 配置

| ALU编号 | 功能 | 延迟 | 执行的指令类型 |
|---------|------|------|----------------|
| **ALU0** | Simple | 1-cycle | ADD/SUB/AND/OR/XOR/SLT/SLL/SRL/SRA |
| **ALU1** | Simple | 1-cycle | 移位+逻辑操作 |
| **ALU2** | Simple | 1-cycle | 地址计算/快速算术 |
| **ALU3** | Simple | 1-cycle | 备用算术单元 |

#### 6.1.2 加法器架构

| 加法器类型 | 架构 | 延迟 | 用途 |
|-----------|------|------|------|
| **快速加法器** | Kogge-Stone (KSA) | ~0.9ns | 关键路径（地址计算、比较） |
| **次级加法器** | 超前进位 (CLA) | ~2.8ns | 非关键路径（溢出处理、次级算术） |

**加法器分配策略：**
- 地址计算 → KSA
- 条件比较 → KSA
- 常规算术 → KSA优先，拥堵时降级为CLA
- 溢出检测 → CLA

#### 6.1.3 详细规格

| 参数 | 值 | 说明 |
|------|-----|------|
| **位宽** | 64 bits | 64位整数操作 |
| **支持操作** | 加/减/与/或/异或/移位/比较 | 基础ALU操作 |
| **多路选择器** | 优先KSA，降级CLA | 双路选择 |
| **标志生成** | Zero/Negative/Overflow/Carry | 状态标志 |

### 6.2 乘法单元 (Multiplier)

#### 6.2.1 总体架构

| 参数 | 配置 | 说明 |
|------|------|------|
| **数量** | 2 个乘法单元 | 双乘法器配置 |
| **复用策略** | 压缩树(CT) + 最终加法器(CPA) 复用 | 面积增加~75% |
| **编码方式** | Radix-4 Booth | 部分积减少75% |
| **压缩树** | Wallace Tree | 快速压缩 |
| **流水线** | 3-stage | 部分积生成→压缩→加法 |
| **MUX策略** | 静态MUX | 选择复用路径 |

#### 6.2.2 详细规格

| 参数 | 值 | 说明 |
|------|-----|------|
| **位宽** | 64 × 64 bits | 64位乘法 |
| **延迟** | 3 cycles | 流水线总延迟 |
| **吞吐量** | 1 operation/cycle | 每周期可启动新乘 |
| **部分积** | 16 组 (Radix-4) | 2×64/8 |
| **压缩器** | 3:2 + 4:2 混合 | Wallace树结构 |

#### 6.2.3 乘法单元复用设计

```
┌─────────────────────────────────────────────────────────┐
│                    乘法单元共享结构                       │
├─────────────────────────────────────────────────────────┤
│                                                         │
│  MUL_A: [PPG_A] ──→ [Static MUX] ──→ [Shared CT]      │
│                          ↓                    │          │
│                         [Shared CPA] ←───────┘          │
│                                                         │
│  MUL_B: [PPG_B] ──→ [Static MUX] ──→ [Shared CT]      │
│                          ↓                               │
│                         [Shared CPA]                     │
│                                                         │
│  PPG: Partical Product Generator (独立)                 │
│  CT:  Compressor Tree (共享)                            │
│  CPA: Carry Propagate Adder (共享)                      │
│                                                         │
└─────────────────────────────────────────────────────────┘
```

| 组件 | MUL_A | MUL_B | 共享 |
|------|-------|-------|------|
| PPG (Booth编码) | ✅ 独立 | ✅ 独立 | ❌ |
| 压缩树 (CT) | ❌ | ❌ | ✅ 共享 |
| 最终加法器 (CPA) | ❌ | ❌ | ✅ 共享 |
| 控制逻辑 | 独立 | 独立 | 部分共享 |

#### 6.2.4 MAC 支持

| 特性 | 支持 | 说明 |
|------|------|------|
| **MAC 指令** | ✅ | 乘加操作 |
| **数据路径** | 直连反馈 | 减少延迟 |
| **向量化MAC** | ✅ | 支持向量MAC |

### 6.3 除法单元 (Divider)

#### 6.3.1 总体架构

| 参数 | 配置 | 说明 |
|------|------|------|
| **算法** | Newton-Raphson | 迭代除法算法 |
| **队列深度** | 8 entries | 后台队列化执行 |
| **执行方式** | 后台执行 | 不阻塞前端流水线 |
| **迭代次数** | ~10-15 次 | 配合乘法器 |

#### 6.3.2 详细规格

| 参数 | 值 | 说明 |
|------|-----|------|
| **位宽** | 64 bits | 64位除法 |
| **初始估计** | 查表 + 多项式 | 快速倒数估计 |
| **每迭代** | 2次乘法 | Newton-Raphson迭代 |
| **平均延迟** | ~20-30 cycles | 平均执行时间 |
| **精度** | 精确舍入 | 符合IEEE 754 |

#### 6.3.3 除法队列设计

```
┌─────────────────────────────────────────────┐
│              Divider Queue (8 entries)       │
├─────────────────────────────────────────────┤
│                                             │
│  Entry 0: [dividend][divisor][状态]         │
│  Entry 1: [dividend][divisor][状态]         │
│  ...                                        │
│  Entry 7: [dividend][divisor][状态]         │
│                                             │
│  状态: IDLE → CALCULATING → COMPLETE        │
│                                             │
│  执行单元:                                   │
│  ┌─────────────────────────────────────┐    │
│  │ 倒数估计 → Newton-Raphson迭代     │    │
│  │ (使用共享乘法器)                     │    │
│  └─────────────────────────────────────┘    │
│                                             │
└─────────────────────────────────────────────┘
```

### 6.4 加载/存储单元 (LSU)

#### 6.4.1 LSU 配置

| 单元 | 数量 | 功能 | 延迟 |
|------|------|------|------|
| **Load Unit** | 2 | 地址计算 + L1访问 | 2-cycle (命中) |
| **Store Unit** | 1 | 地址计算 + L1写入 | 2-cycle |

#### 6.4.2 详细规格

| 参数 | 值 | 说明 |
|------|-----|------|
| **地址计算** | 专用加法器 | 基址+偏移计算 |
| **L1D访问** | 4-way banked | 并发访问支持 |
| **Load Queue** | 16 entries | OoO加载队列 |
| **Store Queue** | 16 entries | OoO存储队列 |
| **Write Buffer** | 16 entries | 写缓冲 |
| **Forwarding** | Store-to-Load转发 | 同地址快速路径 |

### 6.5 分支单元 (BRU)

#### 6.5.1 BRU 配置

| 单元 | 功能 | 延迟 | 说明 |
|------|------|------|------|
| **快速BRU** | 条件分支比较 + 跳转 | 1-cycle | 关键路径优化 |
| **常规BRU** | 分支比较 + 地址计算 | 1-cycle | 备用分支单元 |

#### 6.5.2 分支类型处理

| 分支类型 | 处理单元 | 说明 |
|---------|---------|------|
| 条件分支 (BEQ/BNE等) | 快速BRU | 快速比较 |
| 无条件跳转 (JAL) | 快速BRU | 快速跳转 |
| 间接跳转 (JALR) | 快速BRU + BTB | 目标地址预测 |
| 函数调用 (CALL) | 快速BRU + RAS | 返回地址栈 |
| 函数返回 (RET) | 快速BRU + RAS | 返回地址预测 |

### 6.6 向量单元 (VPU)

#### 6.6.1 VPU 配置

| 参数 | 配置 | 说明 |
|------|------|------|
| **架构** | 共享VPU | 复用整数ALU资源 |
| **执行方式** | 时分复用 | 多周期执行 |
| **最小支持** | Zve64x | ELEN=64, VLEN=64 |
| **元素宽度** | 8-64 bits (动态) | 通过vsetvl配置 |

#### 6.6.2 向量操作支持

| 操作类型 | 支持 | 说明 |
|---------|------|------|
| **向量加/减** | ✅ | 整数/浮点 |
| **向量乘** | ✅ | 复用乘法器 |
| **向量MAC** | ✅ | 乘加操作 |
| **向量移位** | ✅ | 左移/右移 |
| **向量比较** | ✅ | 大小/等于 |
| **向量压缩/扩展** | ✅ | 类型转换 |
| **向量归约** | ✅ | 求和/最大/最小 |

#### 6.6.3 执行延迟

| 操作类别 | 延迟范围 | 说明 |
|---------|---------|------|
| **向量算术** | 4-8 cycles | 取决于向量长度 |
| **向量乘法** | 6-10 cycles | 调用共享乘法器 |
| **向量MAC** | 8-12 cycles | 乘加操作 |
| **向量加载/存储** | 4-8 cycles | 内存访问 |

---

## 7. 乱序核心设计

### 7.1 ROB (Reorder Buffer)

#### 7.1.1 ROB 配置

| 参数 | 配置 | 说明 |
|------|------|------|
| **容量** | 96 entries | ROB总容量 |
| **宽度** | 4 instructions/commit | 每周期提交4条 |
| **附加机制** | Group Commit + Checkpoint | 高效提交与回滚 |

#### 7.1.2 ROB 状态机

| 状态 | 描述 |
|------|------|
| **EMPTY** | ROB条目空闲 |
| **ALLOCATED** | 指令分配到ROB |
| **EXECUTING** | 指令正在执行 |
| **COMPLETE** | 指令执行完成 |
| **READY_TO_COMMIT** | 指令就绪可提交 |
| **COMMITTED** | 指令已提交 |

#### 7.1.3 Group Commit 设计

| 参数 | 值 | 说明 |
|------|-----|------|
| **分组大小** | 4 entries | 每组4条指令 |
| **提交条件** | 组内所有指令COMPLETE | 批量提交 |
| **效率提升** | +15% 有效容量 | 减少ROB空洞 |

#### 7.1.4 Checkpoint 设计

| 参数 | 值 | 说明 |
|------|-----|------|
| **检查点数量** | 4 entries | 并发检查点 |
| **保存内容** | PC + 寄存器映射 | 快速回滚 |
| **恢复时间** | 5-10 cycles | 快速恢复 |
| **使用场景** | 分支预测错误恢复 | 加速回滚 |

### 7.2 寄存器重命名

#### 7.2.1 物理寄存器文件 (PRF)

| 参数 | 配置 | 说明 |
|------|------|------|
| **整型寄存器** | 192 entries | 包含2×32架构寄存器 |
| **浮点寄存器** | 96 entries | 包含2×32架构寄存器 |
| **访问延迟** | ~1.2ns | 单周期访问 |
| **结构** | Banked (4 banks) | 减少访问冲突 |

#### 7.2.2 RAT (Register Alias Table)

| 参数 | 配置 | 说明 |
|------|------|------|
| **架构寄存器映射** | 64 entries (RISC-V) | x0-x31 × 2线程 |
| **映射策略** | 直接映射 + 自由列表 | 高效映射管理 |
| **更新延迟** | 1-cycle | 重命名阶段完成 |

#### 7.2.3 自由列表 (Free List)

| 参数 | 配置 | 说明 |
|------|------|------|
| **深度** | ~128 entries | 可用PRF编号 |
| **分配策略** | FIFO | 先进先出分配 |
| **释放时机** | ROB提交时 | 指令提交后释放 |

### 7.3 调度器 (Scheduler)

#### 7.3.1 调度器配置

| 参数 | 配置 | 说明 |
|------|------|------|
| **架构** | 集中式调度器 | 统一调度 |
| **选择策略** | Age-based + 优先级 | 年龄优先+功能单元优先 |
| **唤醒机制** | Wake-up + 选择 | 快速唤醒就绪指令 |
| **发射窗口** | 96 entries (ROB) | 调度范围 |

#### 7.3.2 功能单元分配

| 指令类型 | 可用单元 | 优先级 |
|---------|---------|--------|
| ALU简单操作 | ALU0, ALU1, ALU2, ALU3 | 均衡分配 |
| 乘法操作 | MUL_A, MUL_B | 交替使用 |
| 除法操作 | Divider Queue | 按需分配 |
| 分支操作 | Fast BRU, Regular BRU | 优先快速BRU |
| 加载操作 | Load0, Load1 | 交替使用 |
| 存储操作 | Store0 | 单一单元 |
| 向量操作 | Shared VPU | 复用ALU/MUL |

### 7.4 同时多线程 (SMT)

#### 7.4.1 SMT 配置

| 参数 | 配置 | 说明 |
|------|------|------|
| **线程数** | 2 threads | T0 + T1 |
| **架构状态** | 2 套完整状态 | PC/RAT/CSR独立 |
| **物理资源** | 共享 | 执行单元/ROB/PRF |

#### 7.4.2 线程选择策略

| 策略 | 说明 |
|------|------|
| **Round-Robin** | 轮询分配 |
| **负载感知** | 根据线程就绪指令数 |
| **公平性保证** | 避免线程饥饿 |

#### 7.4.3 SMT 资源分配

| 资源 | T0 | T1 | 总计 |
|------|-----|-----|------|
| **ROB** | 48 entries | 48 entries | 96 entries |
| **Integer PRF** | 96 entries | 96 entries | 192 entries |
| **Float PRF** | 48 entries | 48 entries | 96 entries |
| **Load Queue** | 8 entries | 8 entries | 16 entries |
| **Store Queue** | 8 entries | 8 entries | 16 entries |

---

## 8. 缓存子系统设计

### 8.1 L1 Cache

#### 8.1.1 L1 数据缓存 (L1D)

| 参数 | 配置 | 说明 |
|------|------|------|
| **容量** | 32 KB | 每线程16KB |
| **相联度** | 8-way | 八路组相联 |
| **行大小** | 64 bytes | 缓存行大小 |
| **组数** | 64 sets | 32K/(8×64) |
| **Tag宽度** | 40 bits | 标签位宽 |
| **访问延迟** | 4 cycles (命中) | Load-to-use延迟 |
| **Bank数** | 4 banks | 支持并发访问 |
| **替换策略** | Pseudo-LRU | 伪LRU替换 |
| **写策略** | Write-Back | 回写策略 |

#### 8.1.2 L1 指令缓存 (L1I)

| 参数 | 配置 | 说明 |
|------|------|------|
| **容量** | 64 KB | 单线程共享 |
| **相联度** | 4-way | 四路组相联 |
| **行大小** | 64 bytes | 缓存行大小 |
| **组数** | 256 sets | 64K/(4×64) |
| **Tag宽度** | 38 bits | 标签位宽 |
| **访问延迟** | 4 cycles (命中) | 取指延迟 |
| **Bank数** | 4 banks | 支持并发取指 |
| **替换策略** | Pseudo-LRU | 伪LRU替换 |
| **写策略** | Read-Only | 只读缓存 |

#### 8.1.3 L1 Cache 特性

| 特性 | 支持 | 说明 |
|------|------|------|
| **非阻塞访问** | ✅ | Miss时不阻塞CPU |
| **MSHR追踪** | ✅ | 支持未完成Miss追踪 |
| **请求合并** | ✅ | 同地址请求合并 |
| **预取支持** | ✅ | 硬件预取指示 |
| **Cache维护** | ✅ | CBO指令支持 |
| **ECC保护** | 🔲 可选 | 错误检测纠正 |

### 8.2 L2 Cache

#### 8.2.1 L2 配置

| 参数 | 配置 | 说明 |
|------|------|------|
| **容量** | 512 KB | 单核心私有 |
| **相联度** | 8-way | 八路组相联 |
| **行大小** | 64 bytes | 缓存行大小 |
| **组数** | 1024 sets | 512K/(8×64) |
| **Tag宽度** | 32 bits | 标签位宽 |
| **访问延迟** | 10 cycles (命中) | L2访问延迟 |
| **Bank数** | 4 banks | 分bank设计 |
| **替换策略** | Pseudo-LRU | 伪LRU替换 |
| **写策略** | Write-Back | 回写策略 |
| **组织方式** | Unifies (I+D共享) | 统一缓存 |

#### 8.2.2 L2 Cache 特性

| 特性 | 支持 | 说明 |
|------|------|------|
| **非阻塞访问** | ✅ | 支持并发Miss |
| **MSHR** | ✅ | 8 entries MSHR |
| **L1-L2 互联** | ✅ | 64B/cycle 总线 |
| ** Victim Cache** | ❌ | 不需要，L2容量足够 |
| **预取引擎** | 🔲 可选 | 硬件预取器 |
| **ECC保护** | ✅ | 单比特纠错 |

### 8.3 MSHR 配置

| 参数 | 配置 | 说明 |
|------|------|------|
| **MSHR总数** | 8 entries | L1D MSHR |
| **L1I MSHR** | 4 entries | 指令缓存MSHR |
| **L2 MSHR** | 8 entries | L2级MSHR |
| **请求合并** | ✅ | 相同地址合并 |
| **追踪方式** | 显式寻址 | 存储完整地址 |

### 8.4 预取器

| 预取类型 | 配置 | 说明 |
|---------|------|------|
| **L2 预取器** | 自适应预取器 | 基于历史模式 |
| **预取深度** | 4-8 行 | 预取深度 |
| **触发条件** | Cache Miss | Miss触发预取 |
| **RISC-V提示** | Zicbop指令 | 软件预取提示 |

### 8.5 存储队列

| 队列 | 容量 | 说明 |
|------|------|------|
| **Load Queue** | 16 entries | OoO加载队列 |
| **Store Queue** | 16 entries | OoO存储队列 |
| **Write Buffer** | 16 entries | L2写缓冲 |

---

## 9. 内存管理单元

### 9.1 TLB (Translation Lookaside Buffer)

| 参数 | 配置 | 说明 |
|------|------|------|
| **页表格式** | Sv39 | 39位虚拟地址 |
| **L1 TLB (ITLB)** | 32 entries, fully-associative | 指令TLB |
| **L1 TLB (DTLB)** | 64 entries, 4-way | 数据TLB |
| **L2 TLB (共享)** | 128 entries, 8-way | 共享L2 TLB |
| **页大小** | 4KB / 2MB / 1GB | 多种页大小支持 |

### 9.2 页表遍历

| 参数 | 配置 | 说明 |
|------|------|------|
| **页表级数** | 3-level | Sv39三级页表 |
| **硬件遍历** | ✅ | 硬件自动遍历 |
| **页表缓存** | ✅ | PTE Cache加速 |
| **访问延迟** | ~20-30 cycles (Miss) | 页表遍历延迟 |

### 9.3 地址空间

| 参数 | 配置 | 说明 |
|------|------|------|
| **虚拟地址空间** | 512 GB (2^39) | Sv39 |
| **物理地址空间** | 1 TB (2^40) | 支持1TB物理地址 |
| **ASID** | 支持 | 地址空间标识符 |
| **VM模式** | Bare / Sv39 | 裸机或Sv39 |

---

## 10. 总线接口

### 10.1 总线协议

| 参数 | 配置 | 说明 |
|------|------|------|
| **协议标准** | AXI5 | 最新AXI协议 |
| **总线宽度** | 128 bits | 数据总线宽度 |
| **地址宽度** | 40 bits | 地址总线宽度 |
| **频率** | 与核心同频或半频 | 可配置 |

### 10.2 AXI5 特性

| 特性 | 支持 | 说明 |
|------|------|------|
| **Outstanding事务** | 支持 | 多未完成事务 |
| **地址突发** | INCR/WRAP/FIXED | 多种突发类型 |
| **Cache一致性** | 支持 (可选) | 跨核一致性 |
| **QoS** | 支持 | 服务质量 |
| **User信号** | 支持 | 扩展信号 |

### 10.3 内存带宽

| 路径 | 带宽 | 说明 |
|------|------|------|
| **L1D ↔ L2** | 64 B/cycle | 每周期64字节 |
| **L2 ↔ AXI** | 128 bits/cycle | AXI总线带宽 |
| **峰值带宽** | 16-32 GB/s | 基于频率计算 |

---

## 11. 时钟与功耗管理

### 11.1 时钟域

| 时钟域 | 频率 | 说明 |
|--------|------|------|
| **前端域** | 1-2 GHz | 取指/解码 |
| **执行域** | 1-2 GHz | ALU/LSU等 |
| **后端域** | 1-2 GHz | ROB/提交 |
| **L2域** | 1 GHz (半频) | L2缓存 |
| **AXI域** | 可配置 | 外部总线 |

### 11.2 功耗管理

| 特性 | 支持 | 说明 |
|------|------|------|
| **时钟门控** | ✅ | 单元级时钟门控 |
| **DVFS** | ✅ | 动态电压频率调节 |
| **Idle状态** | ✅ | WFI/WFE低功耗 |
| **功耗监测** | ✅ | 性能计数器 |

### 11.3 状态管理

| 状态 | 功耗等级 | 唤醒时间 |
|------|---------|---------|
| **Run** | 100% | - |
| **Idle (WFI)** | 10-20% | 即时 |
| **Sleep** | 1-5% | 100-1000 cycles |
| **Deep Sleep** | < 1% | 毫秒级 |

---

## 12. 调试与追踪

### 12.1 调试功能

| 功能 | 支持 | 说明 |
|------|------|------|
| **硬件断点** | ✅ | 指令/数据断点 |
| **单步执行** | ✅ | 逐条指令执行 |
| **寄存器查看** | ✅ | 所有寄存器访问 |
| **内存访问** | ✅ | 内存读写访问 |
| **JTAG接口** | ✅ | 标准调试接口 |

### 12.2 指令追踪

| 功能 | 支持 | 说明 |
|------|------|------|
| **指令追踪** | ✅ | 记录执行指令 |
| **分支追踪** | ✅ | 分支方向记录 |
| **数据流追踪** | 🔲 可选 | 数据访问记录 |
| **压缩存储** | ✅ | 追踪数据压缩 |

### 12.3 性能监测

| 计数器 | 支持 | 说明 |
|--------|------|------|
| **Zicount** | ✅ | 标准性能计数器 |
| **Cache计数器** | ✅ | 命中/Miss统计 |
| **分支计数器** | ✅ | 预测准确率统计 |
| **能量计数器** | ✅ | 能耗估算 |

---

## 13. 编码规范与风格

### 13.1 语言与版本

| 参数 | 规范 |
|------|------|
| **主要语言** | SystemVerilog |
| **标准版本** | IEEE 1800-2017 |
| **补充标准** | IEEE 1800-2020 (部分特性) |

### 13.2 命名规范

| 类型 | 规范 | 示例 |
|------|------|------|
| **模块名** | PascalCase, ripple_前缀 | ripple_alu, ripple_mul |
| **信号名** | snake_case, _i/_o/_s后缀 | clk_i, data_o, valid_s |
| **参数名** | UPPER_CASE, PARAM_前缀 | PARAM_DATA_WIDTH |
| **局部参数** | UPPER_CASE, LOCAL_前缀 | LOCAL_NUM_BANKS |
| **包名** | ripple_前缀, snake_case | ripple_pkg, ripple_axi_pkg |
| **接口名** | ripple_前缀, _if后缀 | ripple_l1d_if |
| **typedef** | UPPER_CASE_T | DATA_W_T, STATE_E |

### 13.3 格式规范

| 参数 | 规范 |
|------|------|
| **缩进** | 4个空格，不使用Tab |
| **行宽** | ≤ 120 字符 |
| **空行** | 逻辑块间2空行，块内1空行 |
| **注释** | `//` 单行，`/* */` 多行 |
| **文件头** | 标准版权和文件说明头 |

### 13.4 电路编码规范

| 参数 | 规范 |
|------|------|
| **时钟** | 统一 `clk` 信号 |
| **复位** | 同步低有效 `rst_n` |
| **寄存器** | `always_ff @(posedge clk)` |
| **组合逻辑** | `always_comb` |
| **FIFO** | 使用标准IP或自定义 |
| **SRAM** | 使用标准单元库 |
| **MUX** | 使用 `unique case` |

### 13.5 文件组织

```
ripple_cpu/
├── rtl/
│   ├── core/
│   │   ├── ripple_pkg.sv          # 包定义
│   │   ├── ripple_top.sv          # 顶层模块
│   │   ├── ripple_frontend.sv     # 前端
│   │   ├── ripple_exec.sv         # 执行单元
│   │   ├── ripple_backend.sv      # 后端
│   │   └── ripple_lsu.sv          # 加载存储单元
│   ├── alu/
│   │   ├── ripple_alu.sv
│   │   ├── ripple_adder_kogge_stone.sv
│   │   └── ripple_adder_cla.sv
│   ├── multiplier/
│   │   ├── ripple_mul.sv
│   │   ├── ripple_booth_encoder.sv
│   │   ├── ripple_wallace_tree.sv
│   │   └── ripple_final_adder.sv
│   ├── divider/
│   │   ├── ripple_div.sv
│   │   └── ripple_nr_iter.sv
│   ├── cache/
│   │   ├── ripple_l1d.sv
│   │   ├── ripple_l1i.sv
│   │   ├── ripple_l2.sv
│   │   └── ripple_mshr.sv
│   ├── predictor/
│   │   ├── ripple_tage.sv
│   │   ├── ripple_perceptron.sv
│   │   ├── ripple_ras.sv
│   │   └── ripple_btb.sv
│   ├── regfile/
│   │   ├── ripple_regfile.sv
│   │   └── ripple_rat.sv
│   ├── tlbt/
│   │   ├── ripple_itlb.sv
│   │   ├── ripple_dtlb.sv
│   │   └── ripple_l2tlb.sv
│   ├── axi/
│   │   ├── ripple_axi_if.sv
│   │   └── ripple_axi_bridge.sv
│   └── debug/
│       ├── ripple_jtag.sv
│       └── ripple_trace.sv
├── tb/
│   ├── ripple_tb_top.sv
│   └── tests/
├── doc/
│   └── PRD.md (本文件)
└── README.md
```

### 13.6 代码示例规范

```systemverilog
// =============================================================================
// Module: ripple_alu
// Description: 64-bit ALU with dual adder support
// Author: RIPPLE Team
// =============================================================================

module ripple_alu (
    input  logic                clk_i,
    input  logic                rst_n_i,
    
    // Input operands
    input  logic [63:0]         operand_a_i,
    input  logic [63:0]         operand_b_i,
    input  logic                carry_in_i,
    
    // Control signals
    input  logic [PARAM_OP_WIDTH-1:0] op_select_i,
    input  logic                use_fast_adder_i,  // KSA or CLA
    
    // Output results
    output logic [63:0]         result_o,
    output logic                carry_out_o,
    output logic                zero_flag_o,
    output logic                overflow_flag_o,
    output logic                negative_flag_o
);
    
    // ... implementation
    
endmodule: ripple_alu
```

---

## 14. 验证策略

### 14.1 验证方法论

| 方法 | 应用 | 说明 |
|------|------|------|
| **定向测试** | 功能模块验证 | 基础功能正确性 |
| **随机测试** | 全系统验证 | 覆盖边界情况 |
| **形式验证** | 关键模块 | 证明正确性 |
| **覆盖率驱动** | 功能覆盖率 | 确保充分验证 |

### 14.2 验证环境

| 组件 | 配置 | 说明 |
|------|------|------|
| **仿真器** | Synopsys VCS/Cadence Xcelium | 工业级仿真器 |
| **验证框架** | UVM (Universal Verification Methodology) | 标准验证方法 |
| **参考模型** | RISC-V ISA模拟器 | Difftest验证 |
| **覆盖率** | 代码覆盖率 + 功能覆盖率 | 全面覆盖 |

### 14.3 关键验证点

| 模块 | 验证重点 |
|------|---------|
| **分支预测器** | 各种分支模式准确率 |
| **执行单元** | 指令正确性、边界条件 |
| **乱序核心** | 依赖跟踪、ROB管理 |
| **缓存子系统** | 一致性、MSHR管理 |
| **SMT** | 线程调度、资源分配 |
| **中断处理** | 中断响应、上下文切换 |

---

## 15. 待确认事项

### 15.1 需确认的技术决策

| 编号 | 决策项 | 暂定方案 | 需确认 |
|------|--------|---------|--------|
| 1 | L1 Cache 配置 | L1D:32KB 8-way, L1I:64KB 4-way | ⬜ |
| 2 | L2 Cache 配置 | 512KB 8-way 统一 | ⬜ |
| 3 | MSHR 数量 | 8 entries (L1D) | ⬜ |
| 4 | 向量单元配置 | 共享VPU, Zve64x | ⬜ |
| 5 | Zfinx vs 标准浮点 | Zfinx (整数寄存器存浮点) | ⬜ |
| 6 | ECC保护 | L2 ECC, L1 无ECC | ⬜ |
| 7 | 预取器 | 自适应L2预取器 | ⬜ |

### 15.2 开放问题

1. **是否需要支持双精度浮点 (Zfa) 扩展？**
2. **是否需要支持向量 Zfhmin (半精度) 扩展？**
3. **L1 Cache 是否需要支持 ECC 保护？**
4. **是否需要硬件预取器 (L2级)？**
5. **是否需要支持 Sv48 页表 (当前选Sv39)？**

### 15.3 后续工作

- [ ] 完成所有技术决策确认
- [ ] 详细模块设计规范
- [ ] 微架构详细设计文档 (MICROARCH)
- [ ] RTL编码实现
- [ ] 验证环境搭建
- [ ] FPGA原型验证

---

## 附录

### A. 设计参数汇总表

| 类别 | 参数 | 值 |
|------|------|-----|
| **总体** | ISA | RV64G + V + C |
| | 发射宽度 | 4-wide |
| | 流水线级数 | ~15-20 |
| | 工作频率 | 1-2 GHz |
| **前端** | 分支预测 | TAGE-SC-L + Perceptron |
| | BTB | 4K entries |
| | RAS | 32 entries |
| | 取指宽度 | 4 instr/cycle |
| **执行** | ALU | 4 × 64-bit |
| | 乘法器 | 2 × 64×64, 3-stage pipeline |
| | 除法器 | 1 × Newton-Raphson, 8-entry queue |
| | 向量单元 | Shared VPU, Zve64x |
| | 加载单元 | 2 × Load |
| | 存储单元 | 1 × Store |
| | 分支单元 | 2 × BRU (fast + regular) |
| **后端** | ROB | 96 entries |
| | 整型PRF | 192 entries |
| | 浮点PRF | 96 entries |
| | SMT | 2 threads |
| **缓存** | L1D | 32KB 8-way |
| | L1I | 64KB 4-way |
| | L2 | 512KB 8-way |
| | MSHR | 8 entries (L1D) |
| **内存** | TLB (ITLB) | 32 entries |
| | TLB (DTLB) | 64 entries |
| | Sv39 | 3-level page table |
| **总线** | 协议 | AXI5 |
| | 数据宽度 | 128 bits |
| **调试** | 接口 | JTAG |
| | 追踪 | 指令追踪支持 |

### B. 术语表

| 缩写 | 全称 | 说明 |
|------|------|------|
| OoO | Out-of-Order Execution | 乱序执行 |
| ROB | Reorder Buffer | 重排序缓冲区 |
| RAT | Register Alias Table | 寄存器别名表 |
| PRF | Physical Register File | 物理寄存器文件 |
| MSHR | Miss Status Holding Register | 未完成Miss保持寄存器 |
| BTB | Branch Target Buffer | 分支目标缓冲 |
| RAS | Return Address Stack | 返回地址栈 |
| SMT | Simultaneous Multithreading | 同时多线程 |
| VPU | Vector Processing Unit | 向量处理单元 |
| LSU | Load/Store Unit | 加载存储单元 |
| BRU | Branch Unit | 分支单元 |
| MAC | Multiply-Accumulate | 乘加操作 |

---

**文档结束**

> 本PRD为 RIPPLE CPU 核心的高层设计规范。如需修改或补充，请在"待确认事项"章节中记录或联系设计团队。
